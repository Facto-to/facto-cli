# Gemini CLI — facto-cli

Project rules + first-day onboarding live in **`AGENTS.md`** at the repo root.

For first-day setup: `bash scripts/dev/init.sh`.

If a rule conflicts between files, AGENTS.md wins.
