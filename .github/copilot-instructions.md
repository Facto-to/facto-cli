# GitHub Copilot — facto-cli

Project rules + contributor onboarding live in **`AGENTS.md`** at the repo root.

For first-day setup: `bash scripts/dev/init.sh`. Project constraints: `../facto-engine/docs/harness/PROJECT_CONSTRAINTS.md`.

This file is a thin pointer; if any rule conflicts between docs, AGENTS.md wins.
