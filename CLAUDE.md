# CLAUDE.md — facto-cli

Claude Code-specific augmentation. Project-wide rules live in **`AGENTS.md`** at the repo root — read that first.

## Claude-specific shortcuts

- `/onboard`    contributor first-day setup (wraps `scripts/dev/init.sh`)
- `/preflight`  env-var presence check (wraps `scripts/dev/preflight.sh`)

## Conflict resolution

If `AGENTS.md` and `CLAUDE.md` disagree, `AGENTS.md` wins.
